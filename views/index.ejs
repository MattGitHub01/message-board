<html>
    <body>
        <h2>Message Board</h2>
        <ul>
            <% messages.forEach((message) => { %>
                <%- include('message', {message:message}); %>
            <% }); %>
        </ul>
        <a href="/new">New Message</a>
    </body>
</html>